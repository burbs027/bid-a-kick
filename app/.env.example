# Copy to .env.local for local overrides, or set in Vercel/hosting dashboard

# Backend API base (include /api)
VITE_API_URL=http://localhost:5000/api

# Socket.io server root (no /api)
VITE_SOCKET_URL=http://localhost:5000
